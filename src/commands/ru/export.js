import ru from './ru.js';

export default {
    ru: {
        description: '*ru `campus`*- send the available menu of the selected campus (if empty, defaults to centro politécnico)',
        execute: ru
    }
}
